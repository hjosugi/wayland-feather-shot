#!/usr/bin/env python3
"""Launcher for wayland-feather-shot (works from the repo or installed)."""
import os
import sys

base = os.path.dirname(os.path.dirname(os.path.realpath(__file__)))
sys.path.insert(0, os.path.join(base, "src"))

from wayland_feather_shot.cli import main  # noqa: E402

sys.exit(main())
